# StaticDrop Web

Next.js web console for StaticDrop.

## Development

```bash
# From repo root
pnpm install
pnpm --filter web dev
```

Set `NEXT_PUBLIC_DEPLOY_TOKEN` and `API_URL` in `.env` for local dev.
